// ローディング画面用
export default function Loading() {

    return (
        <div className="flex justify-center items-center">
            <p className="text-[50px] font-weight text-stone-400 animate-pulse">Now Loading...</p>
        </div>
    );
}